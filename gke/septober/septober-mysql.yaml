# septober-mysql.yaml
# https://hub.docker.com/_/mysql/
# Crea sicuro:
#  1. docker run --name mysql-riccardo-secure -e MYSQL_RANDOM_ROOT_PASSWORD=yes -e MYSQL_ONETIME_PASSWORD=yes -d mysql/mysql-server
#  2. docker logs mysql-riccardo-secure